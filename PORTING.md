# 移植审查与进度

当前参考版本：Unity Classic 1.4.3。已推进地图错位修复，以及试玩反馈的贴图、UI、BSODA 和音效问题。

## 全项目复查与续移植（2026-09-16）

本轮以 `D:\Github\baldibasicForGM` 为移植目标，Unity 原工程只作读取参考。检查运行中的 scripts／objects、数据导入器、资源注册、现有回归、菜单入口和资源生命周期。现有显示数据与当前导入器不一致，已重新生成并通过源文件核对；旧 `gm_runtime_dump.txt` 和历史测试日志不能代表本轮结果。

| 系统 | 本轮处理及当前范围 |
| --- | --- |
| 场景／渲染 | 同步 761 组环境网格、72 个装饰精灵、511 个去重碰撞盒。修复无贴图材质使用空字符串作为 struct 字段而在 Draw 事件崩溃的问题。 |
| 拾取／设施 | 低矮课桌继续阻挡移动，但不遮挡位于其上的拾取射线；贩卖机按与实体碰撞共用的正面盒命中购买；隐藏 NPC 不参与交互。笔记本距离由 NotebookScript 的场景字段导入，道具按 PickupScript 的固定 10 Unity 单位检查。 |
| 导航／生命周期 | 入口升降只更新动态碰撞并复用已构建导航图，不再在答错或每个出口触发时重建高密度网格。NPC 使用轴向地板中心路径，避免对角切角进入装饰物碰撞盒。笔记本路线检查验证可站立的靠近位置和真实交互射线。返回标题时释放地图与寻路 DS 句柄并置为无效，防止句柄复用时误删新地图。 |
| Endless | 模式选择、第二本不可解第三题、后续普通题、每次答错永久怒气 +1、全对怒气 -1、最低怒气 0.5、按源场景参数随时间递增怒气、远离笔记本才累计 120 秒重生、独立计数与最高分。七本不会开启出口阶段。 |
| NPC／语音 | Playtime 的 Ready Go／数字／Oops／Congrats／Sad，校长的 No Bullying 与接触移除 Bully，1st Prize 的 Found／Lost／Hug，以及 Crafters 的追逐循环。NPC 声音在移动时更新距离音量。 |
| 菜单／设置 | 标题、模式选择、操作说明、设置、退出可用，支持鼠标与方向键／Enter。鼠标灵敏度、音量和最高分以 INI 保存。HUD 加入黑色准心，1st Prize 使用源工程 16 个方向精灵。菜单的功能布局已接入，未宣称与 Unity MainMenu 逐像素一致。 |

Endless 依据 `NotebookScript.cs`、`MathGameScript.cs`、`BaldiScript.cs`、`GameControllerScript.cs` 与 `ScoreScript.cs`。Baldi 的 `angerRate=0.01`、`angerRateRate=0.00025`、`angerFrequency=1`、`timeToAnger=0` 和速度缩放由 School 场景直接导入。笔记本重生距离包含被收走后下降到 Unity Y=-20 的高度，答题、暂停期间不推进世界计时；首次全对只调整存储怒气，不激活追逐。

验证使用 gm-cli 2.3.0 的 **compile 和 run**，固定 `GMS2@2026.0.0.23`。最新结果为 **619 项真实 Runner 检查，0 失败**，含学校场景绘制、全部既有显示检查、四个菜单页面渲染、1st Prize 方向精灵和返回标题后重新初始化学校。最新 Runner 中首次答错切换约 5.1 ms，首次入口处理约 4.1 ms，三个出口触发约 4.0–4.5 ms；120 帧 AI 画像平均约 1.18 ms、峰值约 16.9 ms。15 项 Python 回归与地图／玩法／显示／118 个音频文件的一致性检查全部通过。这些是固定 Runner 场景的 CPU 检查，不能换算成整局帧率保证。

本轮真实运行曾捕获并修复纯色网格的 Draw 崩溃。gm-cli 在设置 `CI=1` 时改用工程内 runtime 缓存，导致缺少 receipt；测试脚本现在沿用普通本机环境和已安装的固定 runtime。超时或失败会保留本次日志，并终止该次 gm-cli 的子进程树。普通游戏不启用自检，测试也不会写入玩家设置或最高分。

仍有明确未完成项：

- `Secret`／`TestRoom` 特殊场景、全错特殊结局及答题彩蛋尚未接入。
- 主菜单、失败／结算流程、NPC 朝向精灵与其余动画仍需逐帧／逐像素还原。
- Sweep 对所有 NPC 的推挤、1st Prize 的完整拥抱／刹车细节，以及游戏手柄／移动端输入尚未完整对齐。
- 室外可探索区域仍未完整移植。原工程 `Environment/GeologyRoom/Objects/Chair (33)/Seat` 引用的 mesh GUID `4d7c1677b1627914cbcfa55acd72889a` 在源 Assets 中找不到对应文件，导入报告保留这一缺失；未用猜测网格替代。

以下各节保留此前轮次的修复依据和历史验证数字，以本节作为最新状态。

## 寻路到达时的 NaN 修复

已在真实 Runner 复现 `bb_nav_advance → bb_move_slide` 的 `Cannot apply sqrt to NaN`：寻路返回当前位置后方向长度为零，GameMaker 的浮点比较容差使严格判断 `length < 0.00001` 漏过零长度，归一化产生 `0 / 0`。到达和未移动判断改为包含等号，并在移动与寻路入口拒绝 NaN／无穷大方向。新增 6 项检查覆盖精确到达、微小距离、后续正常移动、无效位移以及 Baldi 到达听觉目标。最新 VM 编译及 **586 项 Runner 检查通过**。

## 场景细节与出口流程修复

`import_unity_details.py` 由显示导入器调用，读取设施的子网格、碰撞盒、四组 Entrance 网格、NearTrigger／ExitTrigger、地图墙材质，以及鞋和跳绳 HUD 资源。运行时 `scr_bb_details` 绘制这些网格并在状态变化时刷新碰撞；入口网格从原静态渲染批次和静态墙碰撞中移出，避免重复绘制和旧墙残留。

入口沿 Y 轴下移 2 个 GameMaker 单位，对应原版下移 10 个 Unity 单位。首次答错后封墙，完成七本后抬回；靠近前三个入口时使用源 NearTrigger 的包围盒提前封闭，给指定墙面换上该入口的地图材质。检测包含上一帧到本帧的移动线段，封闭时必要地将玩家放回墙的走廊侧。最后一个入口保持开启，只有实际 ExitTrigger 才触发结算。

此前通关调用 `audio_stop_all()` 后，同一帧仍执行 `bb_audio_update()`，停止的第三阶段噪声被当成正常播完而重新启动循环。现在通关清空队列与噪声句柄，游戏和音频更新都终止场景处理，再播放原 Results 场景的 `great.wav`。第三阶段切换同时停止上一阶段循环，并等待原版前奏时长和实例播放完成，防止提前重叠。

答题机外壳现在遮住白底、脸部和结果标记的外沿；物品栏导入 ItemBG 并先于选中底色和边框绘制。电话导入 SpriteRenderer 的原始比例；贩卖机使用原版三个固定网格和盒碰撞。防滑鞋在 0–2 秒穿上、2–15 秒隐藏、15–17 秒脱下，保护持续 15 秒。跳绳恢复原文 `Time to jump rope!` 和独立计数；留堂恢复居中红字 `You have detention! / N seconds remain!`，均采用源 TMP 字号、颜色和布局。

## 答题机第二轮修复

原导入器只取有贴图的部分控件，漏掉 MathGame 预制体的纯色 `BG`、`Image`、`TextBG` 和输入框白底。现在按原矩形补齐这些层，使场景完全被遮住。题目原来经通用文本函数居中且字号不一致，答案坐标又误用了输入框父物体；现在读取实际 TMP 文本控件，使用原版 COMIC_Pro 字体图集、字形尺寸、基线、行高和左上对齐。长答案按输入视口裁剪、滚动，乱码题使用原版三个重叠文本层。

BaldiFeed 原先放入全身挥手精灵。现从 `BaldiFeed.controller` 与动画曲线导入待机、说话和皱眉帧及状态速度。说话由当前答题音频驱动，首次答错保留脸部并播放皱眉；以后进入笔记本时隐藏脸部，与 MathGameScript 的逻辑一致。

新增 `import_unity_audio.py`，完整导入原工程 118 个 AudioClip，保留源 GUID／路径／SHA-256，恢复既有资源名称并生成直接资源引用。学习音乐按源 AudioSource 使用 0.7 音量。首次答错后，答题语音同时在队列入口和播放入口检查追逐状态；答题中关闭 F1 菜单只恢复本次答题的声音实例，防止恢复其他场景音效。

本轮源文件核对没有发现既有 WAV 被替换成其他文件；进入笔记本的 Runner 测试只记录到原版学习音乐与介绍语音，没有触发闹钟铃声。另以已经响铃的世界闹钟验证打开答题、开关作弊菜单期间保持暂停。音频资源导入完成不代表所有尚未移植的 NPC 状态和结局触发逻辑已经接入。

## 测试工具

`scr_bb_debug` 提供 F1 作弊菜单，四页分别为玩家开关、物品、地图传送和流程控制。菜单在游戏更新前接管输入，在 GUI 最后绘制；打开期间停止游戏推进并暂停声音。关闭时按原状态恢复鼠标与声音，答题期间只恢复答题音频，保持世界音频暂停。

调试标记放在每局的 `global.G.debug` 中，重新开始学校即重置。传送列表直接读取当前地图，门使用其真实中心确定朝向；穿墙关闭和传送会检查墙体、门及地板。设置笔记本数量会同步拾取状态、入口显示和出口进度。物品不消耗只影响使用，商店购买会保留硬币并将商品放入其他槽位。

作弊菜单的 69 项真实 GML 检查覆盖默认关闭、游戏／原暂停／答题状态的菜单开关、音频暂停层次、无敌切换、穿墙及安全退出、十种道具、BSODA 消耗切换、笔记本与门锁状态、AI 暂停、结束答题、全部门和笔记本传送，以及各菜单页按钮的屏幕边界。

## 地图问题与修复依据

旧导入器根据 `Hall_*` 父物体的 yaw 猜测门所在墙面，并将所有门放在地块边缘正中。实际 `_DoorIn`／`_DoorOut` 与 `barrier` 子物体具有独立的位置、旋转和缩放：教室门宽度为半个地块，并偏在一侧。旧的墙面导入还将半墙扩成整墙，再整面挖洞，造成左右颠倒、门口封死和错误缺口。

具体锚点（均为 GameMaker 世界坐标）：

| 源物体 | 正确门中心 X/Z | 说明 |
| --- | --- | --- |
| Hall_2Wall_Door (1) | -1 / -3.5 | 出生区左侧教室门 |
| Hall_2Wall_Door | 1 / -6.5 | 出生区右侧教室门 |
| Hall_2Wall_Door (3) | 5 / -31.5 | 校长办公室门 |
| Hall_SwingDoor_Side | 5 / -58 | 食堂东侧入口 |
| Hall_SwingDoor_Side (1) | -21 / -58 | 食堂西侧入口 |
| Room_1Wall_FacultyDoor | -11 / -12.5 | 旧地图遗漏的教职工门 |

`Cafe_Door` 是食堂墙体模块，没有门脚本。旧工具把它及其副本当成了额外双开门。食堂高处的墙原先也被压到地面高度，形成不应存在的障碍。

新版 `tools/unity_scene.py` 组合父级的平移、四元数旋转与缩放。`tools/import_unity_map.py` 读取 Unity 内置 Plane／Quad 的实际顶点，以门脚本的 `barrier` 和 `outside` 引用分别确认碰撞与贴图朝向。转换为 `gx=(ux-5)*0.2, gy=uy*0.2, gz=1-uz*0.2`。

`datafiles/school_map.json` 的 schema 2 保留每面墙的顶点、包围盒、材质及源 Transform ID。门同时保留地块位置和真实中心、宽度、顶点、脚本 ID。代码不会对这些精确平面执行旧的整墙挖洞或自动补对称门框。导航边保存实际门洞中心；楼上墙只参与绘制，不变成地面的碰撞墙。

已导入 23 扇脚本门、682 个地板区域和 7 个笔记本。全部地板区域在门可通行时连通。`tools/unity_map_report.json` 记录生成统计，`tools/unity_map_comparison.svg` 保留本轮修复前后门位置对照。

## 已接入的玩法

早期工程已有第一人称 3D 绘制、体力、答题、Baldi 追逐、6 类 NPC 框架与四出口流程。本轮在用户将重点调整为地图前，还接入了 Unity 的 16 个拾取点、5 个交互设施和十种道具，并改为带失败重计数的跳绳。

门锁、钥匙、四次静音开门、硬币购买、电话／磁带的 30 秒听觉干扰、30 秒后响铃的闹钟、剪刀与 15 秒靴子效果已有实现。新增拾取和道具使用共用射线目标，避免隔墙拾取。该部分的完整原版行为、音频及视觉还需逐项试玩对照。

## 验证范围与剩余工作

试玩反馈的修复依据：

- 校规与黑板的镜像来自把 Unity Plane 当作 Quad 使用同一套 U 坐标。Plane 的 U 方向与 Quad 相反，已在导出时分别处理，保留原来的正反面裁剪。
- 物品／笔记本的错误图像来自 3D billboard 固定采样 0..1，取到了整张纹理页。现在通过 `sprite_get_uvs` 同时处理图集采样与透明裁边。各房间笔记本的真实封面通过 SpriteRenderer 引用取得，不再显示通用笔记本再染色。
- 物品栏原先裁切背景，却用另一组缩放坐标放图标。答题机的按钮、文字、结果标记原先使用猜测坐标。新版 `presentation.json` 从原 HUD 与 MathGame 预制体导入矩形、资源和尺寸，由同一组数据绘制并处理点击。
- 3D 绘制结束时原先自行设置居中的正交矩阵，会影响后续绘制。现在保存并恢复调用前的三个矩阵。
- 出口标志此前画在出口触发器的位置，落在墙后。现在读取五个实际 ExitSign 的位置和尺寸；属于入口动态物体的标志仍随入口状态显示／隐藏。
- BSODA 原先只在 NPC 移动后追加一段位移，无法压制追逐，还可能晚于死亡判定。现在先检测喷雾接触，接触期间替代 NPC 移动，并检查 Baldi 快速移动是否跨过喷雾。喷雾本身按原触发器保留 30 秒生命，角色仍受墙体碰撞限制。
- 第一题答错只增加临时怒气时，拍尺等待值曾保持为零。现在激活追逐即初始化等待时间，并防止拍尺事件逐帧触发。答题、校长与导师语音使用独立队列；答题提交、首次答错、退出答题机都会清理相应语音。恢复原版结束／处分音效，移除错误复用的 `ohno` 和拾取铃声，门声按距离衰减。

真实 GameMaker Runner 的自检覆盖门几何与碰撞对齐、锁门阻挡玩家及 NPC、开门双向通行、30/60/144 FPS 下经过偏置门洞的寻路、出生点到全部笔记本的路线、材质加载和正面裁剪。Python 检查包含独立的出生区、办公室、食堂坐标锚点，并支持重新读取 Unity 比对导入结果。

场景细节轮验证结果：GameMaker 2026.0.0.23 VM 编译通过；当轮真实 Runner 的 580 项检查全部通过；10 个 Python 显示回归测试、显示资源源文件一致性检查通过；此前全部音频一致性检查和 5 个地图回归测试通过。该轮新增 37 项 Runner 检查覆盖物品栏白底、答题机外壳遮挡、四个出口牌在真实场景中的可见性、入口下移／恢复碰撞、提前封闭及高速跨越防卡住、第三阶段音频切换、通关声音清理和鞋动画时序。像素比较使用一次 GPU 读取后批量比较，保持检查在自动运行的时间限制内。

这轮自动检查不等同于整局人工视觉验收。以下工作尚未宣称完成：

- NPC 的完整原版状态机、动画与角色间推挤细节（本轮已补关键语音、Bully／校长处理）。
- 其余室外可探索区域与缺失的源网格（本轮已同步环境家具与装饰数据）。
- 双面门贴图细节和其余界面动画的逐帧对照。
- 特殊结局与菜单的原版视觉；Endless 和 PC 设置功能已在本轮接入。

继续地图工作时应通过新版导入器修改来源与规则，避免重新运行旧的坐标猜测脚本覆盖结果。
