# 移植审查与进度

当前参考版本：Unity Classic 1.4.3。已推进地图错位修复，以及试玩反馈的贴图、UI、BSODA 和音效问题。

## 测试工具

`scr_bb_debug` 提供 F1 作弊菜单，四页分别为玩家开关、物品、地图传送和流程控制。菜单在游戏更新前接管输入，在 GUI 最后绘制；打开期间停止游戏推进并暂停声音。关闭时按原状态恢复鼠标与声音，答题期间只恢复答题音频，保持世界音频暂停。

调试标记放在每局的 `global.G.debug` 中，重新开始学校即重置。传送列表直接读取当前地图，门使用其真实中心确定朝向；穿墙关闭和传送会检查墙体、门及地板。设置笔记本数量会同步拾取状态、入口显示和出口进度。物品不消耗只影响使用，商店购买会保留硬币并将商品放入其他槽位。

新增 69 项真实 GML 检查覆盖默认关闭、游戏／原暂停／答题状态的菜单开关、音频暂停层次、无敌切换、穿墙及安全退出、十种道具、BSODA 消耗切换、笔记本与门锁状态、AI 暂停、结束答题、全部门和笔记本传送，以及各菜单页按钮的屏幕边界。最新 Runner 总计 **517 项检查通过**，并生成菜单运行截图。

## 地图问题与修复依据

旧导入器根据 `Hall_*` 父物体的 yaw 猜测门所在墙面，并将所有门放在地块边缘正中。实际 `_DoorIn`／`_DoorOut` 与 `barrier` 子物体具有独立的位置、旋转和缩放：教室门宽度为半个地块，并偏在一侧。旧的墙面导入还将半墙扩成整墙，再整面挖洞，造成左右颠倒、门口封死和错误缺口。

具体锚点（均为 GameMaker 世界坐标）：

| 源物体 | 正确门中心 X/Z | 说明 |
| --- | --- | --- |
| Hall_2Wall_Door (1) | -1 / -3.5 | 出生区左侧教室门 |
| Hall_2Wall_Door | 1 / -6.5 | 出生区右侧教室门 |
| Hall_2Wall_Door (3) | 5 / -31.5 | 校长办公室门 |
| Hall_SwingDoor_Side | 5 / -58 | 食堂东侧入口 |
| Hall_SwingDoor_Side (1) | -21 / -58 | 食堂西侧入口 |
| Room_1Wall_FacultyDoor | -11 / -12.5 | 旧地图遗漏的教职工门 |

`Cafe_Door` 是食堂墙体模块，没有门脚本。旧工具把它及其副本当成了额外双开门。食堂高处的墙原先也被压到地面高度，形成不应存在的障碍。

新版 `tools/unity_scene.py` 组合父级的平移、四元数旋转与缩放。`tools/import_unity_map.py` 读取 Unity 内置 Plane／Quad 的实际顶点，以门脚本的 `barrier` 和 `outside` 引用分别确认碰撞与贴图朝向。转换为 `gx=(ux-5)*0.2, gy=uy*0.2, gz=1-uz*0.2`。

`datafiles/school_map.json` 的 schema 2 保留每面墙的顶点、包围盒、材质及源 Transform ID。门同时保留地块位置和真实中心、宽度、顶点、脚本 ID。代码不会对这些精确平面执行旧的整墙挖洞或自动补对称门框。导航边保存实际门洞中心；楼上墙只参与绘制，不变成地面的碰撞墙。

已导入 23 扇脚本门、682 个地板区域和 7 个笔记本。全部地板区域在门可通行时连通。`tools/unity_map_report.json` 记录生成统计，`tools/unity_map_comparison.svg` 保留本轮修复前后门位置对照。

## 已接入的玩法

早期工程已有第一人称 3D 绘制、体力、答题、Baldi 追逐、6 类 NPC 框架与四出口流程。本轮在用户将重点调整为地图前，还接入了 Unity 的 16 个拾取点、5 个交互设施和十种道具，并改为带失败重计数的跳绳。

门锁、钥匙、四次静音开门、硬币购买、电话／磁带的 30 秒听觉干扰、30 秒后响铃的闹钟、剪刀与 15 秒靴子效果已有实现。新增拾取和道具使用共用射线目标，避免隔墙拾取。该部分的完整原版行为、音频及视觉还需逐项试玩对照。

## 验证范围与剩余工作

试玩反馈的修复依据：

- 校规与黑板的镜像来自把 Unity Plane 当作 Quad 使用同一套 U 坐标。Plane 的 U 方向与 Quad 相反，已在导出时分别处理，保留原来的正反面裁剪。
- 物品／笔记本的错误图像来自 3D billboard 固定采样 0..1，取到了整张纹理页。现在通过 `sprite_get_uvs` 同时处理图集采样与透明裁边。各房间笔记本的真实封面通过 SpriteRenderer 引用取得，不再显示通用笔记本再染色。
- 物品栏原先裁切背景，却用另一组缩放坐标放图标。答题机的按钮、文字、结果标记原先使用猜测坐标。新版 `presentation.json` 从原 HUD 与 MathGame 预制体导入矩形、资源和尺寸，由同一组数据绘制并处理点击。
- 3D 绘制结束时原先自行设置居中的正交矩阵，会影响后续绘制。现在保存并恢复调用前的三个矩阵。
- 出口标志此前画在出口触发器的位置，落在墙后。现在读取五个实际 ExitSign 的位置和尺寸；属于入口动态物体的标志仍随入口状态显示／隐藏。
- BSODA 原先只在 NPC 移动后追加一段位移，无法压制追逐，还可能晚于死亡判定。现在先检测喷雾接触，接触期间替代 NPC 移动，并检查 Baldi 快速移动是否跨过喷雾。喷雾本身按原触发器保留 30 秒生命，角色仍受墙体碰撞限制。
- 第一题答错只增加临时怒气时，拍尺等待值曾保持为零。现在激活追逐即初始化等待时间，并防止拍尺事件逐帧触发。答题、校长与导师语音使用独立队列；答题提交、首次答错、退出答题机都会清理相应语音。恢复原版结束／处分音效，移除错误复用的 `ohno` 和拾取铃声，门声按距离衰减。

真实 GameMaker Runner 的自检覆盖门几何与碰撞对齐、锁门阻挡玩家及 NPC、开门双向通行、30/60/144 FPS 下经过偏置门洞的寻路、出生点到全部笔记本的路线、材质加载和正面裁剪。Python 检查包含独立的出生区、办公室、食堂坐标锚点，并支持重新读取 Unity 比对导入结果。

最新验证结果：GameMaker 2026.0.0.23 VM 编译通过；真实 Runner 的 448 项检查全部通过；10 个 Python 地图与显示回归测试通过；地图和显示数据的 Unity 源场景一致性检查通过，包括导入图片逐字节一致性。新增检查直接比较 3D 图集精灵与 2D 参考渲染，检查物品栏背景、答题按钮像素和点击区域、实际出口标志、BSODA 接触与高速穿越，以及拍尺频率和语音取消。

这轮自动检查不等同于整局人工视觉验收。以下工作尚未宣称完成：

- NPC 的完整原版状态机、其余 NPC 的语音队列、Bully 拦路与校长违规区域逻辑。
- 出口下沉场景的动态几何、室外区域、课桌与其他场景物件的完整移植。
- 双面门贴图细节、界面动画和答题语音的逐帧对照。
- Endless 模式、设置、结局与完整菜单流程。

继续地图工作时应通过新版导入器修改来源与规则，避免重新运行旧的坐标猜测脚本覆盖结果。
